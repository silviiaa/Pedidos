{% extends 'base.html.twig' %}

{% block title %}PedidoProducto{% endblock %}

{% block body %}
    <h1>PedidoProducto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CodPedProd</th>
                <td>{{ pedido_producto.codPedProd }}</td>
            </tr>
            <tr>
                <th>Unidades</th>
                <td>{{ pedido_producto.unidades }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pedido_producto_index') }}">back to list</a>

    <a href="{{ path('pedido_producto_edit', {'codPedProd': pedido_producto.codPedProd}) }}">edit</a>

    {{ include('pedido_producto/_delete_form.html.twig') }}
{% endblock %}
